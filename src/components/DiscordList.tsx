import React, { FC } from "react";
import Box from "@mui/material/Box";
import Button from "@mui/material/Button";

export const DiscordList: FC = () => {
  return (
    <Box sx={{ justifyContent: "center" }}>
      <Button variant="contained">Connect Discord</Button>
    </Box>
  );
};
